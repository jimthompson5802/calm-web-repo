{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://schemas.company.com/data/encryption-at-rest.json",
    "title": "Encryption at Rest Requirement",
    "description": "Defines the required configuration for encrypting stored data at rest",
    "type": "object",
    "properties": {
        "algorithm": {
            "type": "string",
            "enum": ["AES-128-GCM", "AES-256-GCM", "AES-256-CBC"],
            "description": "Encryption algorithm to use for data at rest"
        },
        "key-management": {
            "type": "string",
            "enum": ["HSM-backed", "KMS", "software"],
            "description": "Key management strategy for encryption keys"
        },
        "key-rotation-days": {
            "type": "integer",
            "minimum": 1,
            "maximum": 730,
            "description": "Encryption key rotation interval in days"
        }
    },
    "required": ["algorithm", "key-management", "key-rotation-days"],
    "additionalProperties": false
}
